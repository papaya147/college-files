// 21BDS0340 Abhinav Dinesh Srivatsa

#include <stdio.h>

int main()
{
    int num;
    scanf("%d", &num);
    if (num >= 0)
    {
        printf("accepted");
    }
    else
    {
        printf("Enter only positive number");
    }
}